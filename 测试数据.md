数据设计：原则 平面上ap数>=3，否则无法实现准确定位；三维空间中ap>=4甚至更多
要测试算法各方面功能，应该在原则确定的情况下，把设计中的各个方面进行多种可能的组合测试。
原则：
算法基于平面 或 空间

数据项目：
AP数：3个 或 多个
ap的分布
距离衰减函数：相同 获 不同
阻隔：有 或 没有
阻挡衰减函数：相同 或 不同
手机的信号强度（接收不到的设为0）
手机的位置：移动 或 不移动

这些项目的不同都可以组合成很多组测试数据。